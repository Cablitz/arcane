local ServerStorage = game:GetService("ServerStorage")
local planck = require("@packages/planck")

local dummies = ServerStorage.dummies

local function addDummies()
    
end

return {
    system = addDummies,
}
